const HomePage = () => {
	// const demo = async (e) => {
	// 	e.preventDefault();
	// 	const user = {
	// 		email: "demo@aa.io",
	// 		password: "password",
	// 	};
	// 	setModalOpen(false);
	// 	dispatch(login(user.email, user.password));
	// };

	return (
		<>
			<h1>HomePage</h1>

		</>
	);
};

export default HomePage;
